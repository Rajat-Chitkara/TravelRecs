/** Minimum mention_count for an entity to be promoted onto the ranked list. */
export const RANKING_MIN_MENTIONS = 3;

/** Minimum number of entities with a non-null crowd_signal before the
 * "Crowd level" filter is considered to have enough coverage to show. */
export const CROWD_SIGNAL_MIN_COVERAGE = 15;
